#include "MatrixCalculatorClass.h"    // Include header file containing Matrix Calculator class

int main() {

    MatrixCalculator.menu();          // Prompt user for input and act upon it within an infinite loop

    return 0;
}
